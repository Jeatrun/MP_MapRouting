<!--pages/V0.5/PolyMark.wxml-->
<text>pages/V0.5/PolyMark.wxml</text>
<view class="container">
  <map id="map"
    class="map"
    markers="{{markers}}"
    longitude="{{longitude}}" latitude="{{latitude}}"
    polyline="{{polyline}}"
    scale="20"
    >
    
  </map>
</view>

<button size="mini" bindtap="buttonSearch">检索“图书馆”</button>